import type { TemplateContext, TemplateMeta } from '../context';

export const meta: TemplateMeta = {
    file:    '.claude/skills/continuity/SKILL.md',
    version: 16,
    label:   'continuity skill',
};

const CONTENT = [
    "---",
    "name: continuity",
    "description: Bindery workspace - Cross-check a chapter for consistency errors in characters, world rules, or timeline. Use for /continuity, \"check continuity\", or \"check chapter X for errors\".",
    "---",
    "# Skill: /continuity",
    "",
    "Cross-check a chapter for consistency errors.",
    "",
    "## Prerequisites",
    "This skill requires a Bindery workspace. If unsure, call `bindery_identify_book` to check. If no workspace is found, tell the user and stop.",
    "",
    "## Trigger",
    "User says `/continuity`, \"check continuity\", or \"check chapter X for errors\".",
    "",
    "## Clarify first",
    "- Chapter: number — if not provided, ask before proceeding",
    "- Focus: all | characters | world rules | timeline — if not provided, default to **all**",
    "- Language: use the source language by default; if the user specifies a translation, use that",
    "",
    "## Tools",
    "Use these Bindery MCP tools:",
    "- `bindery_get_text(path)` — read settings and memory files",
    "- `bindery_session_focus_get(section?)` — read the current focus / working state from `SESSION.md`",
    "- `bindery_arc_list` / `bindery_arc_get` — read structured arc files",
    "- `bindery_character_list` / `bindery_character_get` — read structured character profiles",
    "- `bindery_get_chapter(chapterNumber, language)` — read a specific chapter",
    "- `bindery_get_book_until(chapterNumber, language, startChapter?)` — load all prior chapters in one call for timeline checks; fall back to `bindery_get_chapter` for nearby chapters if unavailable",
    "- `bindery_get_notes(category, name)` — look up older character notes (`category: \"Characters\"`) or world rules (`category: \"World\"`)",
    "- `bindery_search(query, language)` — find earlier mentions of a specific detail or event",
    "- `bindery_memory_list` — check whether a chapter-specific memory file exists (`chXX.md`)",
    "",
    "## Steps",
    "",
    "### Load context",
    "1. `bindery_get_text(\".bindery/settings.json\")` — pick up the book's structure and conventions.",
    "2. Use `bindery_arc_list` and `bindery_arc_get` to load the structural map. If the chapter's act is known, read the matching act file.",
    "3. Use `bindery_character_list` to load the cast map.",
    "4. `bindery_get_text(\".bindery/memories/global.md\")` — cross-chapter decisions and established facts. Use `bindery_memory_list` to check if a chapter-specific memory file exists; if so, read it too.",
    "5. `bindery_get_chapter(chapterNumber, language)` — read the chapter to check.",
    "",
    "### Load reference material (by focus area)",
    "6. **Characters**: `bindery_character_get` for relevant structured character profiles; use `bindery_get_notes(category: \"Characters\")` only for older notes.",
    "7. **World rules**: `bindery_get_notes(category: \"World\")` for world and magic rules.",
    "8. **Timeline**: `bindery_get_book_until` up to the focus chapter for continuity context.",
    "9. For any focus: use `bindery_search` to verify specific details against earlier chapters.",
    "",
    "### Check and report",
    "10. Compare the chapter against the loaded reference material.",
    "11. Report findings using the output format below.",
    "",
    "## Output format",
    "",
    "| Type | Location | Issue | Reference |",
    "|---|---|---|---|",
    "| Character | Para 3 | Description contradicts... | global.md |",
    "",
    "- **Confirmed inconsistency** — flag as an error",
    "- **Uncertain** — flag as a question, marked with `?` in the Type column",
    "",
    "End with a one-line overall assessment. If no issues found, say so clearly.",
    "",
    "## Rules",
    "- Reporting only — do not suggest rewrites",
    "- Limit checks to the requested focus area; if focus is \"all\", cover all three categories",
].join('\n') + '\n';

export function render(_ctx: TemplateContext): string {
    return CONTENT;
}
