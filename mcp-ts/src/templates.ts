/**
 * Bindery AI instruction file templates — thin re-export shim.
 *
 * The single source of truth has moved to `bindery-core/src/templates.ts`.
 * This file simply re-exports everything so that existing imports
 * (`import ... from './templates.js'`) continue to work without change.
 */

export * from '@bindery/core';
