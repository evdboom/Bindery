{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "compile-vscode-ext",
            "type": "shell",
            "command": "npx tsc -p ./",
            "options": { "cwd": "${workspaceFolder}/vscode-ext" },
            "group": "build",
            "presentation": { "reveal": "silent" },
            "problemMatcher": "$tsc"
        }
    ]
}
